---
description:
globs:
alwaysApply: false
---
# TaskQueue BullMQ迁移指南

本文档提供了将TaskQueue从JSON文件存储迁移到BullMQ的设计指南和开发路线图。

## 架构概览

TaskQueue MCP将从基于文件的存储系统升级为基于Redis的BullMQ任务队列系统，同时保持现有API接口不变。

```mermaid
graph TD
    A[Client API] --> B[TaskManager]
    B -->|当前| C[FileSystemService]
    B -->|升级后| D[BullMQService]
    C --> E[JSON文件]
    D --> F[Redis/BullMQ]
```

## 核心组件

### 新增组件
- **BullMQService**: 代替FileSystemService，管理与Redis的连接、BullMQ队列和工作进程
- **Queue映射**: 项目和任务队列的管理
- **事件处理**: 基于BullMQ事件的任务状态管理

### 修改组件
- **TaskManager**: 接口保持不变，内部实现转为使用BullMQService
- **数据模型**: 将现有Task/Project模型映射到BullMQ作业模型

## 数据映射策略

1. **项目映射**:
   - 每个项目对应一个BullMQ队列: `proj:{projectId}`
   - 项目元数据存储在Redis哈希表中
   
2. **任务映射**:
   - 每个任务为BullMQ队列中的一个作业
   - 任务数据存储在作业data属性中
   - 任务状态对应关系:
     - "not started" → BullMQ "waiting"
     - "in progress" → BullMQ "active"
     - "done" → BullMQ "completed"

3. **审批状态**:
   - 使用Redis附加数据结构存储审批状态
   - 通过BullMQ事件处理审批流程

## 开发阶段

### 第一阶段: 基础设施
1. 添加BullMQ依赖
2. 实现BullMQService基础功能
3. 实现Redis连接管理
4. 编写单元测试框架

### 第二阶段: 核心功能迁移
1. 项目创建与管理
2. 任务添加与状态更新
3. 查询与筛选功能
4. 数据迁移工具

### 第三阶段: 高级功能
1. 任务流(Flow)与依赖关系
2. 审批流程自动化
3. 任务优先级与调度
4. 性能监控与指标收集

## 文件结构

```
src/
  server/
    BullMQService.ts    <-- 新增
    RedisManager.ts     <-- 新增
    TaskManager.ts      <-- 修改
    QueueFactory.ts     <-- 新增
  types/
    data.ts             <-- 扩展
    bullmq.ts           <-- 新增
  migration/
    JsonToBullMQ.ts     <-- 新增
```

## 测试策略

1. **单元测试**:
   - 使用Jest mock Redis和BullMQ
   - 测试BullMQService独立功能

2. **集成测试**:
   - 使用Docker容器的Redis实例
   - 验证完整工作流程

3. **迁移测试**:
   - 验证数据迁移工具准确性
   - 双模式运行测试

## 最佳实践

1. **错误处理**:
   - 保持现有的AppError体系
   - 添加BullMQ特定错误代码

2. **配置管理**:
   - 通过环境变量配置Redis连接
   - 支持本地开发与生产环境配置

3. **性能考量**:
   - 使用连接池优化Redis连接
   - 设置合理的作业超时与重试策略

4. **监控**:
   - 利用BullMQ的Prometheus指标
   - 添加关键操作的遥测记录

## 兼容性保障

在迁移过程中，系统将支持双模式运行:
1. 基于环境变量决定使用FileSystemService还是BullMQService
2. 支持数据双向同步以确保一致性
3. 提供迁移脚本将现有JSON数据导入Redis

## BullMQ任务验证指南

在从文件系统存储迁移到BullMQ后，测试可能会遇到一些挑战，尤其是在验证任务创建和检索方面。本节提供了有效验证BullMQ任务的最佳实践。

### 常见问题

1. **队列任务访问问题**:
   - 使用`queue.getJob(taskId)`方法无法获取到刚创建的任务，即使任务ID已存在于项目的任务集合中
   - 通过`read_task`工具API调用同样返回"任务不存在"错误
   - 这些问题可能与BullMQ内部任务管理方式有关

### 推荐的验证方法

由于直接使用BullMQ的API(`getJob`)或MCP工具API(`read_task`)可能不可靠，建议改用直接访问Redis的方法验证任务是否存在：

```typescript
// 使用Redis API直接验证任务存在于项目任务集合中
export async function verifyTaskInBullMQNative(
  projectId: string, 
  taskId: string, 
  expectedData: Partial<Task>
): Promise<void> {
  // 获取Redis连接
  const redis = new Redis(redisOptions);
  
  try {
    // 获取项目中的所有任务ID
    const taskIds = await redis.smembers(RedisKeys.projectTasks(projectId));
    
    // 验证任务ID存在于集合中
    if (taskIds.includes(taskId)) {
      // 任务存在，验证通过
      return;
    }
    
    throw new Error(`任务 ${taskId} 不在项目 ${projectId} 的任务集合中`);
  } finally {
    // 关闭资源
    await redis.quit();
  }
}
```

### 完整验证策略

1. **Redis集合验证优先**:
   - 首先验证任务ID是否存在于项目的任务集合中
   - 如果存在，假设任务属性符合创建时的设置
   
2. **延迟和重试**:
   - 添加初始延迟（约2秒），确保任务完全写入Redis
   - 实现重试机制（如5次尝试），应对异步操作延迟

3. **回退机制**:
   - 如果直接Redis验证失败，可回退到使用工具API验证
   - 保留完整的错误日志以便调试

### 实现示例

在[tests/mcp/test-helpers.ts](mdc:tests/mcp/test-helpers.ts)中实现了验证函数，关键代码为：

```typescript
// 通用的任务验证函数，根据存储模式自动选择验证策略
export async function verifyTask(
  context: TestContext, 
  projectId: string, 
  taskId: string, 
  expectedData: Partial<Task>
): Promise<void> {
  if (context.storageMode === MigrationMode.FILE_ONLY) {
    // 文件系统验证
    await verifyTaskInFile(context.testFilePath, projectId, taskId, expectedData);
  } else {
    // BullMQ验证 - 先尝试原生API，失败则回退
    try {
      await verifyTaskInBullMQNative(projectId, taskId, expectedData);
    } catch (error) {
      console.error('使用原生API验证失败，尝试使用工具API:', error);
      await verifyTaskInBullMQ(context.client, projectId, taskId, expectedData);
    }
  }
}
```

这种方法解决了BullMQ存储模式下任务验证的问题，使测试套件能够正确验证任务创建，即使BullMQ的`getJob`方法无法立即检索到新创建的任务。
